actions:
- changed-files:
  - any-glob-to-any-file: ['**/action.yml', '**/action.yaml', '.github/**/*']

test:
- changed-files:
  - any-glob-to-any-file: 
    [
      'src/**/*.spec.js',
      'src/**/*.spec.ts',
      'src/**/*.test.js',
      'src/**/*.test.ts',
      'src/**/__tests__/**/*',
      '__tests__/**/*',
      'tests/**/*'
    ]

# Add 'Documentation' label to any changes within 'docs' folder or any subfolders
Documentation:
- changed-files:
  - any-glob-to-any-file: 
    [
      'docs/**/*', 'doc/**/*', 
      'documentation/**/*', 
      '.docs/**/*'
    ]

⚙️ configuration:
- changed-files:
  - any-glob-to-any-file:
    [
      '.idea/**/*',
      '.github/**/*',
      '.husky/**/*',
      '.lintstagedrc',
      '.prettierrc',
      '.eslintrc',
      '.editorconfig',
      '.gitignore',
      '.gitattributes',
      '.npmignore',
      '.nvimrc',
      '.npmrc',
      '*.config.js',
      '*.config.ts'
    ]

📦 dependencies:
- changed-files: 
  - any-glob-to-any-file: ['package.json', 'package-lock.json', 'yarn.lock', 'pnpm-lock.yaml']

# Add 'feature' label to any PR where the head branch name starts with `feature` or has a `feature` section in the name
feature:
 - head-branch: ['^feature', 'feature']

 # Add 'release' label to any PR that is opened against the `main` branch
release:
 - base-branch: 'main'